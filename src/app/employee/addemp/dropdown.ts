/*import { Directive } from '@angular/core';
import { Input} from '@angular/core';
import {HostListner} from '@angular/core';

@Directive({
  selector: 'bs-dropdown',
  host: {'class':'dropdown'}
})
export class BsDropdown {
  @Input() open = false;
}}*/
